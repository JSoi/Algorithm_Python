def solution(s):
    answer = 0
    for len in range(len(s)//2):
        myset = set()
        mystr = s
    def go(length):
        pass
    def change(string, standard):
        if string == standard:
            return string
        else:
            num = string.count(standard)
            return str(num) + standard
        return

    return answer
