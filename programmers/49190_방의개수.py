import collections


def solution(arrows):
    ds = [-1, -1, 0, 1, 1, 1, 0, -1]
    dg = [0, 1, 1, 1, 0, -1, -1, -1]
    # 그림을 그리면서 방 개수를 세야하는 건지
    visitqueue = collections.deque
    visitqueue.append()
    for a in arrows:
       visitqueue[a]
    answer = 0
    return answer
